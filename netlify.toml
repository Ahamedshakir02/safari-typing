# Build + SPA routing for Netlify. Mirrors public/_redirects so deep links work.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
