# Build for Netlify. Every route is prerendered, so there is no SPA rewrite -
# unmatched paths get the real 404 page. Mirrors public/_redirects.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
