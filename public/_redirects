# Every route is prerendered to real HTML, so no SPA rewrite is needed — the
# host serves the matching file directly. Anything that matches no file is a
# genuine 404 and must say so (a 200 rewrite to index.html turns every typo
# into a duplicate of the homepage in Google's index).
/*    /404.html   404
